# The base URL of the backend API
# During local development, this usually points to localhost
# For production (e.g. Netlify), set this in your Netlify dashboard to your Render backend URL
VITE_API_BASE=http://localhost:5000
